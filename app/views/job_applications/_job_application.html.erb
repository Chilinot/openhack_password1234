<a href="#" class="list-group-item">
  <h4 class="list-group-item-heading">Anonymous User</h4>
  <div class="list-group-item-text">
    <% job_application.user.skills.each do |skill| %>
      <%= image_tag skill.image.url(:thumb) %>(<%= UserSkill.skill_level(job_application.user, skill)%>)
    <% end %>
  </div>
</a>
